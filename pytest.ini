[pytest]
testpaths = tests
markers =
    smoke: Quick sanity checks for critical paths
    regression: Full regression suite
    api: Standard API contract tests
    performance: Response time and concurrency tests
    schema: JSON schema validation tests
addopts =
    -v
    --strict-markers
    --tb=short
log_cli = true
log_cli_level = INFO
